name: build

on:
  push:
    branches: [main]
    # NOTE: no paths-ignore. Branch protection on main requires the
    # "Build (Release)" check to pass before merge — paths-ignored events
    # never publish a check at all, which would silently lock out every
    # docs-only PR. We pay ~3 min of CI for the occasional README change
    # so the rule "every PR runs CI" stays true.
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build (Release)
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore StandbyAndTimer/StandbyAndTimer.csproj

      - name: Build (warnings as errors)
        run: dotnet build StandbyAndTimer/StandbyAndTimer.csproj -c Release --no-restore -warnaserror

      - name: Publish (smoke check)
        # No --no-restore: the publish profile pins RuntimeIdentifier=win-x64,
        # which needs a RID-aware restore. The first restore above is RID-less
        # so its assets file would be rejected here.
        run: |
          dotnet publish StandbyAndTimer/StandbyAndTimer.csproj `
            -p:PublishProfile=win-x64-single `
            -p:Version=0.0.0-ci
          Get-Item StandbyAndTimer/bin/publish/win-x64/StandbyAndTimer.exe |
            Select-Object Name, @{Name='SizeMB'; Expression={[math]::Round($_.Length/1MB,1)}}
